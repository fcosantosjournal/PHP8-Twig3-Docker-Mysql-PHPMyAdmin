{% extends "base.html.twig" %} 

{% block content %}
    <!--
    <p>{{routes.path}}</p>
    <p>{{routes.template}}</p>
    <p>{{routes.controller}}</p>
    -->
    <p>{{data.title}}</p>
    <p>{{data.message}}</p>
    <p> 
        {% if routes.routes %}
            {% include 'menu.html.twig' with {'routes': routes.routes} %}
        {% endif %} 
    </p>
{% endblock %} 
